import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-playfair',
})

export const metadata: Metadata = {
  title: 'Allurena - Premium Skincare & Beauty',
  description: 'Enhance the natural beauty of your skin every day with Allurena products. Choose according to the needs of your skin, age and time of use from the range of cleaners, treatments and other products.',
  keywords: 'skincare, beauty, cosmetics, natural, premium, cleanser, treatment, moisturizer',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
} 